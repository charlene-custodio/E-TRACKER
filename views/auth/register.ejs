<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Register | E-TRACKER</title>
  <link rel="stylesheet" href="/css/register.css" />
</head>
<body>
<div class="container">
  <div class="form-box">
    <div class="blue-container">
      <div class="form-header" style="display: flex; gap: 12px;">
        <img src="/images/logo.png" alt="Logo" class="logo" style="height: 48px;"/>
        <div>
          <h2 class="no-margin">E-TRACKER</h2>
          <h4 class="no-margin tagline">anecdotal report and record tracker</h4>
        </div>
      </div>
    </div>
    <h2>Register</h2>

      <% if (error) { %>
        <p class="error"><%= error %></p>
      <% } %>

      <form action="/register" method="POST">
        <div class="input-group">
          <label>Username</label>
          <input name="username" required />
        </div>

        <div class="input-group">
          <label>Password</label>
          <input name="password" type="password" required />
        </div>

        <div class="input-group">
          <label>Full Name</label>
          <input name="full_name" required />
        </div>

        <div class="input-group">
          <label>Role</label>
          <select name="role" required>
            <option value="tutor">Tutor</option>
            <option value="admin">Admin</option>
          </select>
        </div>

        <button type="submit">Register</button>
      </form>

      <p class="login-link">Already have an account? <a href="/login">Login</a></p>
    </div>

    <div class="right">
      <div class="overlay">
        <img src="/images/logo.png" alt="Logo" class="logo" />
        <p class="tagline">
          Your Partner in Fostering Student Progress,<br>
          Monitoring Interventions, and Supporting Growth.
        </p>
        <h1>E-TRACKER</h1>
      </div>
    </div>
  </div>
</body>
</html>
