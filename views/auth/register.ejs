<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Register | E-TRACKER</title>
  <link rel="stylesheet" href="/css/register.css" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700;900&display=swap" rel="stylesheet" />
</head>
<body>
  <div class="register-container">
    <!-- Left Section (Form) -->
    <div class="register-left">
      <div class="register-header">
        <img src="/images/logo.png" alt="E-TRACKER Logo" class="register-main-logo" />
        <div>
          <h1 class="register-title">E-TRACKER</h1>
          <h4 class="register-subtitle">Anecdotal Report & Student Record Tracker</h4>
        </div>
      </div>
      <div class="register-form-section">
        <h2 class="register-form-title">Tutor Register</h2>
        <% if (error) { %>
          <p class="error"><%= error %></p>
        <% } %>
        <form action="/register" method="POST" autocomplete="off">
          <div class="input-group">
            <label>Username</label>
            <input name="username" type="text" required autocomplete="username" placeholder="Username" />
          </div>
          <div class="input-group">
            <label>Password</label>
            <input name="password" type="password" required autocomplete="new-password" placeholder="Password" />
          </div>
          <div class="input-group">
            <label>Full Name</label>
            <input name="full_name" required autocomplete="name" placeholder="Full Name" />
          </div>
          <div class="input-group">
            <label>Role</label>
            <select name="role" required>
              <option value="tutor">Tutor</option>
            </select>
          </div>
          <button type="submit" class="register-btn">Register</button>
        </form>
        <p class="login-link">Already have an account? <a href="/login">Login</a></p>
      </div>
    </div>
    <!-- Right Section (Image and Branding) -->
    <div class="register-right">
      <div class="register-right-overlay">
        <div class="register-right-content">
          <h1 class="register-right-big-title">E-TRACKER</h1>
          <p class="register-right-tagline">
            Your Partner in Fostering Student Progress,<br>
            Monitoring Interventions, and Supporting Growth.
          </p>
        </div>
      </div>
    </div>
  </div>
</body>
</html>